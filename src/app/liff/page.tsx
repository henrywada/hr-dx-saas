// src/app/liff/page.tsx
// LINE LIFF ルータページ（/liff）
// liff.state クエリパラメータを解析して適切なサブルートへリダイレクトする。
import { LiffRouterView } from './LiffRouterView'

export default function LiffRouterPage() {
  return <LiffRouterView />
}
